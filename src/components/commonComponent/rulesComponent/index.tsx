import RightPanelContainer from "../rightPanelContainer";

const RulesComponent = ({ children }: any) => {
  return <RightPanelContainer title={"Rules"}>{children}</RightPanelContainer>;
};

export default RulesComponent;
